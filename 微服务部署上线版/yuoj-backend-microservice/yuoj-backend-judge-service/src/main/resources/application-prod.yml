# 生产环境配置文件
# 
# @from <a href="https://rainbowsea.blog.csdn.net/"> RainbowSea CSDN地址</a>
spring:
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/yuoj
    username: root
    password: MySQL密码xxx
  # Redis 配置
  redis:
    database: 1
    host: redis
    port: 6379
    timeout: 5000
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
  rabbitmq:
    host: rabbitmq
    port: 5672
    password: rabbitmq密码xxx
    username: guest
codesandbox:
  type: remote
  url: http://locahost:9090/executeCode